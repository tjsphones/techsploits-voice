# Copy this file to .env and fill in the values.
# NEVER commit the real .env. These are the same secrets you already have in
# C:\Users\tl\AppData\Local\hermes\.env for SignalWire.

# --- SignalWire (from your Desktop creds file) ---
SIGNALWIRE_PROJECT_ID=
SIGNALWIRE_SPACE_URL=techsploits.signalwire.com
SIGNALWIRE_SIGNING_KEY=
SIGNALWIRE_TOKEN=
SIGNALWIRE_PHONE=406-416-6665

# --- Brain: two-tier router ---
# FRONT (fast, light conversation) = Google Gemma via OpenRouter (free).
FRONT_API_KEY=        # = OPENROUTER_API_KEY
FRONT_MODEL=google/gemma-4-26b-a4b-it:free
FRONT_BASE_URL=https://openrouter.ai/api/v1
# REASON (heavy, your Nous brain, BYOK/private) = tencent/hy3:free via Nous.
REASON_API_KEY=       # = NOUS_API_KEY
REASON_MODEL=tencent/hy3:free
REASON_BASE_URL=https://inference-api.nousresearch.com/v1
# If FRONT can't handle a question it replies ESCALATE and we hand off to REASON.
# Leave FRONT_API_KEY empty to route everything through Nous (max privacy, slower).

# --- Speech-to-text: Deepgram (handles telephony audio natively) ---
DEEPGRAM_API_KEY=

# --- Text-to-speech: elevenlabs | openai | azure ---
TTS_ENGINE=elevenlabs
ELEVENLABS_API_KEY=
# Optional: voice id for elevenlabs; leave blank for default

# --- Mid-call email (SWAIG) ---
SHOP_EMAIL=brent@techsploits.com
# For sending email we relay through your Gmail SMTP (app password), or a
# transactional provider. See README "Email" section.
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_USER=tjsworldmt@gmail.com
EMAIL_SMTP_PASS=

# --- Misc ---
DRY_RUN=false
PORT=8080
